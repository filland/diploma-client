package bntu.diploma;


/**
 *
 * TODO
 *
 * - split login and main controller
 * - API Worker !!!!
 * - HTML report generation
 * - In InteractiveMap -> show all stations's info by a button
 * - Fix exception invoking by clicking stationInfoNode's params
 *  - StationInfoNode's info show be at front
 *  - scaling does not work after moving any dot
 *
 *
 * Not so ASAP:
 * - make moving stationIntoNode's coordinates more accurate while scaling
 *
 *
 * DONE:
 * - change the lower table to a list of fields
 * - scaling windows and dots on it and dots' size (probably) - mostly
 */
public class WhatToDO {
}
